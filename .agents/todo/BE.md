# Backend TODO & Memory List

- [ ] Check lại api call từ nhân viên, check quyền

giới tính: nam, nữ, khác


thous_sep đang là bool. sửa lại thành enum phẩy chấm hoặc ko áp dụng,
bỏ apply_idx
validate time format với kiểu dữ liệu tương ứng
validate tất cả các input truyền lên
các ô nhập có rule thì check theo rule. ko có rule thì vẫn  phải validate giới hạn trần tránh trường hợp nhập 10000 kí tự chẳng hạng
thuộc tính zone 2 ko được nằm trong zone khác, trừ zone ẩn
thuộc tính chọn bắt buộc phải có ít nhất 1 option, tên option cũng bắt buộc cả tiếng anh lẫn tiếng việt

validate khi nào tất cả hợp lệ mới show nút lưu


check lại size tiêu đề modal, 16, các zone nhỏ hơn 14


- FE tổ chức ref component. click view detail (click xem chi tiết nhân viên)



